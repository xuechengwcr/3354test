
                //        err_exit(err, "can’t create thread");
                //        err_exit(err, "can’t create thread");
                //        err_exit(err, "can’t create thread");
                //        err_exit(err, "can’t create thread");
